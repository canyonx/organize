<section class="card">
	<div class="card-header">
		<h2 class="card-title">Participer</h2>
	</div>
	<div class="card-body">
		{{ form_start(form) }}
		{{ form_row(form.message) }}
		<div class="text-end">
			<button class="btn btn-success rounded-pill">{{ button_label|default('Envoyer') }}</button>
		</div>
		{{ form_end(form) }}
	</div>
</section>
