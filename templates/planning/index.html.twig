{% extends 'base.html.twig' %}

{% block title %}Planning
{% endblock %}

{% block body %}
	<section class="card">
		<div class="card-body">
			<h1>Mon planning</h1>
			<div class="row justify-content-between">
				<div class="col-md-auto me-auto my-2">

					<button class="disabled btn">
						Filtrer les sorties
					</button>
					<a href="{{ path('app_planning_index')}}" class="btn btn-primary">
						Toutes
					</a>
					<a href="{{ path('app_planning_index', {'f': 'created'})}}" class="btn btn-primary">
						Crées
					</a>
					<a href="{{ path('app_planning_index', {'f': 'requests'})}}" class="btn btn-primary">
						Demandées
					</a>
				</div>
				<div class="col-md-auto my-2">
					<a href="{{ path('app_trip_new') }}" class="btn btn-primary">
						Nouvelle sortie
					</a>
				</div>
			</div>
		</div>
		<div class="card-body">
			{% include "table/_table.html.twig" %}
		</div>
	</section>

{% endblock %}
