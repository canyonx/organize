{% extends 'base.html.twig' %}

{% block title %}Conditions générales d'utilisation
{% endblock %}

{% block body %}
	<section class="card">
		<div class="card-header">
			<h1 class="card-title">Conditions générales d'utilisation</h1>
		</div>
		<div class="card-body">
			{% for cgu in cgus %}
				<div class="p-3">
					<h2 class="text-primary">{{ cgu.title|capitalize }}</h2>
					<p>{{ cgu.content|raw }}</p>
				</div>
			{% endfor %}
		</div>
	</section>
{% endblock %}
