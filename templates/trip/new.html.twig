{% extends 'base.html.twig' %}

{% block title %}Nouvelle sortie
{% endblock %}

{% block body %}
	<section class="card">
		<div class="card-header">
			<h1>Créer une sortie</h1>
			<a href="{{ path('app_planning_index') }}" class="btn btn-primary rounded-pill">Retour</a>
		</div>
		<div class="card-body">
			{{ include('trip/_form.html.twig') }}
		</div>
	</section>
{% endblock %}
