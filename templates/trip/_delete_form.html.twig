<form method="post" action="{{ path('app_trip_delete', {'id': trip.id}) }}" onsubmit="return confirm('Supprimer la sortie ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ trip.id) }}">
	<button class="btn btn-danger rounded-pill">Supprimer</button>
</form>
