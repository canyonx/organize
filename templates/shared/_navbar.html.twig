<nav class="navbar navbar-expand-lg navbar-light bg-white border-bottom" aria-label="navbar">
	<div class="container-fluid">

		<a
			class="navbar-brand d-flex align-items-center mb-3 mb-md-0 me-md-auto text-primary text-decoration-none" href="{{ path('app_home') }}">
			{# <img src="{{ asset('images/') }}" width="40" height="40" alt="Logo"> #}
			<span class="fs-4">OrganiZe</span>
		</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbar00" aria-controls="navbar00" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>

		<div class="collapse navbar-collapse" id="navbar00">

			<ul class="navbar-nav ms-auto  text-primary">
				{% if is_granted('ROLE_USER') %}
					<li class="nav-item">
						<a href="{{ path('app_planning_index') }}" class="nav-link">Planning</a>
					</li>
					<li class="nav-item">
						<a href="{{ path('app_search_index') }}" class="nav-link">Chercher</a>
					</li>
					<li class="nav-item">
						<a href="{{ path('app_profile_index') }}" class="nav-link">{{ app.user }}</a>
					</li>
					<li class="nav-item">
						<a href="{{ path('app_logout') }}" class="nav-link">Déconnecter</a>
					</li>
				{% else %}
					<li class="nav-item">
						<a href="{{ path('app_register') }}" class="nav-link">Créer un compte</a>
					</li>
					<li class="nav-item">
						<a href="{{ path('app_login') }}" class="nav-link">Connecter</a>
					</li>
				{% endif %}
			</ul>
		</div>

	</div>
</nav>
