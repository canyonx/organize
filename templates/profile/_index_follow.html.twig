<section class="card m-3">
    <div class="card-body">
        <h2>Suivis ({{ user.myFriends|length }})</h2>
        
        <div class="row d-flex flex-row p-3">
            {% for friend in user.myFriends %}
                <div class="col-4">
                    {{ component('UserCard', { user: friend }) }}
                </div>
            {% else %}
                Aucun utilisateur suivi !
            {% endfor %}
        </div>
    </div>
</section>